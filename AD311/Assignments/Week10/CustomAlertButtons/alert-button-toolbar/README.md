# Custom Alert Buttons in React

Practice for passing events and values to child React components through props.

## How To Run

1. Run `npm run dev` in the console.
2. Navigate to `localhost:5174` in your browser.
