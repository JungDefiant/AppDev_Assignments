# Dynamic Alert Buttons with Props

Practice for generating React components dynamically through props.

## How To Run

1. Run `npm run dev` in the console.
2. Navigate to `localhost:5174` in your browser.
