# Integrating Third-Party APIs
This project is for learning how to fetch data from third-party APIs using JavaScript.

A user can use this application to generate a random Dungeons & Dragons character or retrieve information about a specific race, class, or alignment.

To try out the project, run **npm start** in a console at the root folder.
To check that all tests pass, run **npm test** in a console at the root folder.
